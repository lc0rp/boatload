# Dev-docs update 2026-06-22

## Purpose

Initialized the numbered dev-docs information architecture while moving devbox access toward a Caddy edge proxy.

## Changes

- Added `dev-docs/README.md` from the shared dev-docs template.
- Added all numbered lifecycle folders with `index.md` files.
- Added documentation conventions under `dev-docs/00-foundation/conventions/`.
- Added docs validation scripts and package scripts.

## Validation

- Run `npm run lint:links`.
- Run `npm run lint:ia`.
